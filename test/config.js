const config = {
  keycloak: {
    url: 'http://localhost:8080'
  },
  rest: {
    host: 'localhost',
    port: 1080
  },
  client: {
    host: 'localhost',
    port: 4000
  }
}

module.exports = config
